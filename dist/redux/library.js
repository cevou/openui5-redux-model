sap.ui.define(['jquery.sap.global'], function() {
    'use strict';

    sap.ui.getCore().initLibrary({
        name: 'redux',
        version: '0.1.0',
        dependencies: ['sap.ui.core']
    });
});
